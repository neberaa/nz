---
label: Achievements
hide_body: true
fields:
- name: section_title
  type: text
  config:
    required: false
  label: Section title
- name: background_image
  type: file
  config:
    maxSize: 64
  label: Background image
- name: achievements
  type: blocks
  label: Achievements
  template_types:
  - achievement-item
  config:
    min:
    max:
- name: page_key
  type: text
  config:
    required: false
  label: Page key
  hidden: true
  default: achievements
pages:
- data/pageBlocks/achievements.json
