---
label: Courses
hide_body: true
fields:
- name: section_title
  type: text
  config:
    required: false
  label: Section title
- name: header
  type: text
  config:
    required: false
  label: Header
- name: cta_button_text
  type: text
  config:
    required: false
  label: CTA button text
- name: courses
  type: blocks
  label: Courses
  template_types:
  - course-item
  config:
    min: 
    max: 
pages:
- data/pageBlocks/courses.json
